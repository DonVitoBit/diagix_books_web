# Скопируйте в scripts/proxy.env на сервере (не коммитится).
# Пример:
# HTTP_PROXY=http://USER:PASSWORD@HOST:PORT
# HTTPS_PROXY=http://USER:PASSWORD@HOST:PORT
# NO_PROXY=localhost,127.0.0.1,127.0.0.1:8501
